import { StyledTextOrder } from '../OrderList.styled';

const OrderDescriptionMalfunction = ({ value }) => {
  return <StyledTextOrder>Опис несправності: {value}</StyledTextOrder>;
};

export default OrderDescriptionMalfunction;
