import { StyledTextOrder } from '../OrderList.styled';

const OrderDescriptionOfRepair = ({ value }) => {
  return <StyledTextOrder>Опис ремонту: {value}</StyledTextOrder>;
};

export default OrderDescriptionOfRepair;
