import { StyledTextOrder } from '../OrderList.styled';


const OrderDateCreation = ({ value }) => {
  return <StyledTextOrder>Дата створення: {value}</StyledTextOrder>;
};

export default OrderDateCreation;
