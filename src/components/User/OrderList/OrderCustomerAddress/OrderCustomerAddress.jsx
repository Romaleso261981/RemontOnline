import { StyledTextOrder } from '../OrderList.styled';

const OrderCustomerAddress = ({ value }) => {
  return <StyledTextOrder>Адреса : {value}</StyledTextOrder>;
};

export default OrderCustomerAddress;
