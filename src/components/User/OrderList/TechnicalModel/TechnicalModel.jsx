import { StyledTextOrder } from '../OrderList.styled';

const TechnicalModel = ({ model }) => {
  return <StyledTextOrder margin={'16px'}>Модель: {model}</StyledTextOrder>;
};

export default TechnicalModel;
